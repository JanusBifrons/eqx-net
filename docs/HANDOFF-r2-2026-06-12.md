# HANDOFF — Equinox Round 2 roadmap (2026-06-12 EOD)

Pick-up doc for the next session. The durable cross-session record is the memory
file `equinox-round2-execution-status.md` (auto-loaded via `MEMORY.md`); this is
the readable "start here tomorrow" supplement.

## Where we are

Executing the committed **Equinox Round 2 bugfix roadmap**
(`docs/plans/equinox-round2-bugfix-roadmap.md`, on branch
`origin/plan/equinox-round2-bugfix-roadmap`) — 33 Round-2 bugs grouped into
workstreams **WS-1 … WS-14**, each its own branch off `main` + its own PR.

- **WS-1 … WS-11: MERGED** into `main` (currently `2757967`, the PR #40 merge).
- **WS-11 (PR #40) landed all-green.** The netgate RED'd on the FIRST run —
  `rollingCorrRate` HEAD median 0.65 vs baseline 0.35 (past the 0.6 ceiling) — but
  the per-rep values swung 0.0→1.0 (both arms), and the netgate scenario strafes
  the player through a 25-drone pack while firing (`feel-test-25`), so the
  drone-damping change shifts the player↔drone ram-collision corrections in a
  high-variance way. A **re-run came back GREEN** → it was the noisy median, not a
  real regression. The fix stands. (Lesson: `rollingCorrRate` is high-variance in
  the ram scenario; a single RED median is not a verdict — re-run / characterise
  before reacting. Read the metric from the JOB LOG via the Actions API, not the
  empty check-run output.)

## WS-11 (PR #40) — 3 commits

1. **R2.21 — persistent colour-coded warp indicator** (`b01420b`, pure client).
   The incoming-warp banner is now always-mounted (stable empty container when
   idle) and coloured by relation (hostile=red / neutral=amber / friendly=green),
   client-derived (no wire change).
2. **R2.24 Part B — base-ready toast** (`8ff5503`, not netgate). A one-shot
   `base_ready` discrete broadcast → owner-only "Base operational" toast the first
   time a base qualifies for waves. *(User said: "say the word if you'd rather I
   pull it — it mentions waves.")*
3. **R2.25 — drone "float / never approaches" fix** (`0bdd46e`, 🔴 — the headline).

### ⚠️ The one OPEN verdict: R2.25 on-device feel
- **Root cause** (not the standoff logic): drone bodies spawned with
  `setLinearDamping(0)`, and the AI `applyImpulse` path bypasses the player's
  max-speed clamp — so once a drone overshoots its target it **coasts away
  forever** (the standoff brake goes to thrust=0; nothing dissipates the speed).
- **Fix**: drone bodies now get their per-kind `ShipKind.linearDamping` (exactly
  what `spawnShip` already does); asteroids + structures stay ballistic (0).
- **The VALUE is principled (the kind's own catalogue field, not a guess), but
  the FEEL is unverified on-device.** The user bundled it knowingly. **NEXT
  SESSION: after PR #40 merges, get an on-device smoke pass; the damping value
  (and possibly the `HostileDroneBehaviour` inside-brake / maxClosing cap) may
  need a tweak.** Tests are structural (real-Rapier `World.test` + `SwarmSpawner.
  test`) — they do NOT encode a feel threshold, so a value tweak won't fight them.

### ⚠️ DO NOT rebuild the R2.24 "waves never return" latch fix
I built it (a "squad driven off → retreat → re-dispatch") and **reverted it** —
the user corrected the design:
- Waves should **keep coming while you have a base** (continuous pressure when
  you're winning). The grace / de-escalation is ONLY to stand the drones down
  once you **lose** (no Miners + peaceful) — which the existing code already does.
- My fix relented pressure exactly when the player was winning. Backwards.
- The actual on-device symptom ("a non-hostile wave warped in, a huge number of
  ships, then left and never came back") is the **roaming neutral squads**
  drifting the galaxy — not a dispatch latch. **User: "probably just ignore that
  for now."** So the waves-recurrence work is out of scope; roaming is
  deprioritised. Lesson captured in
  `feedback-validate-fix-direction-vs-game-intent.md`.

## NEXT WORK (roadmap order)

1. **R2.25 on-device feel check** (the open verdict above) — PR #40 is merged, so
   this is THE outstanding item for WS-11: smoke the drone behaviour on-device and
   confirm the damping value feels right (drones approach + hold a standoff instead
   of floating away). A value tweak (per-kind `linearDamping`, or the
   `HostileDroneBehaviour` inside-brake / maxClosing cap) is the likely follow-up
   if it's off — the structural tests won't fight a value change.
2. **WS-12** — lingering forever (R2.26) + live re-entry (R2.33) + render (R2.32).
   Per §A of the roadmap, **R2.33 is reproduce-first** (ambiguous reconnect path:
   rebind reads live SAB at `SectorRoom.ts:3232`, fresh-spawn-restore reads the
   frozen Limbo pose at `:3435` — reproduce to find WHICH path, don't assume).
4. **WS-13** — speed-dial UX (R2.6). **WS-14** — warp glow removal / map sharpness
   / curtain (R2.9/7). WS-13/14 are QUICK fillers, interleavable.

## Process notes (how this roadmap runs)

- **Per-WS workflow**: sync to merged `main`, branch `feat/r2-wsNN-...`, run a
  **lean recon→harden Workflow** (3 recon agents + 1 harden, structured JSON
  schemas; surface genuine forks via `AskUserQuestion`), implement **commit-per-
  step**, push, open PR. Keep workflows LEAN.
- **Local loop = typecheck + NEW tests + lint** (+ 8 s server boot for
  server-touching changes). **Defer the full suite + netgate to PR CI.**
- **Reproduce-first** for bug reports (automated/structural repro before guessing;
  NEVER ship a hypothesis-fix for a feel bug). For gameplay/design bugs, validate
  the **fix direction** against the user's intent before building (WS-11 R2.24).
- **PR creation** (no `gh` CLI): `git push -u origin <branch>`, then POST to the
  GitHub API with the token from `git credential fill` — **strip the trailing
  `\r`** (`| tr -d '\r\n'`) or you get "Bad credentials". CI polling: same token →
  `/repos/JanusBifrons/eqx-net/commits/<sha>/check-runs`.
- **Standing**: user merges PRs mid-flight + branches delete on merge (re-push
  re-creates them; rebase the new commit onto fresh main for a clean follow-up).
  Auto-commit consent for this roadmap is standing. User on remote control (phone)
  — cap timeouts on hanging commands.
